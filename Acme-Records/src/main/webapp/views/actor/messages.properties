actor.save			= Save

actor.cancel 		= Cancel

actor.name			= Name

actor.middleName	= Middle name

actor.surname		= Surname

actor.email			= Email

actor.address 		= Address

actor.phoneNumber	= Phone Number

actor.password		= Password

actor.username		= Username

actor.photo			= Photo

actor.creditCard	= Credit card

actor.ban           = Ban

actor.unban         = Unban

actor.delete		= Delete

actor.commit.error = Error 

actor.confirm.delete = Are you sure to delete the profile?

actor.editCreditCard	= Edit credit card

actor.confirm.phone = Are you sure to confirm this phone number?